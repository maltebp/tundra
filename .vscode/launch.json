{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "Model Compiler Test",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/model-compiler/debug/td-model-compiler.exe",
            "args": ["arg1", "arg2"],
            "cwd": "${workspaceFolder}"
        }
    ]
}